let arr = [1, 3, 4, 2, 5,3];
let v = 4;
let flag = 0;

for (let i = 0; i < arr.length; i++) {
  if (v === arr[i]) {
    console.log(`found at ${i}`);

    flag = 1;
    break;
  }
}

if (flag === 0) {
  console.log("not found");
}
